import React from 'react'

import loading from '../resources/spinner.gif'

function LoadingSpinnerComponent (): React.JSX.Element {
  return <img src={loading} alt="loading" />
}

export default LoadingSpinnerComponent
